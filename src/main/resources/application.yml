spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/filetools
    username: filetools
    password: filetools
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true

